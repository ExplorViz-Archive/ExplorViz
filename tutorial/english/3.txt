The systems are made up of nodes (light green) that can be part of a node group (dark green). Inside the nodes you can see the applications (purple) that are currently running on the system.<br>
Node groups consist of duplicate nodes and only show one of those nodes by default.